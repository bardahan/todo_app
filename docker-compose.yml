version: '3.3'

services:
  web:
    build:
      context: todo-front
    ports:
      - "80:8000"
    environment:
      - API_BASE_URL=$API_BASE_URL

  back:
    build:
      context: back
    ports:
      - "8001:8001"
    environment:
      - DB_HOST=$DB_HOST
      - DB_USER=$DB_USER
      - DB_PASS=$DB_PASS
      - DB_SCHEME=$DB_SCHEME
